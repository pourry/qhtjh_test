# 测试环境配置
server:
  port: 8001

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    mode:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/test_all?serverTimezone=UTC&nullCatalogMeansCurrent=true
      username: root
      password: l1998C12r05
    flowable:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/flowable?serverTimezone=UTC&nullCatalogMeansCurrent=true
      username: root
      password: l1998C12r05

# 日志配置
logging:
  level:
    com.example.spring_boot_mode: DEBUG
    org.springframework.test: INFO

# 注意：Flowable 的配置已迁移至 FlowableConfig.java 中手动管理
# FlowableProcessAutoConfiguration 已排除，不会在 test_all 数据库中建表
