<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_boot_mode.model.dao.reminder.ReminderDao">

    <resultMap id="ReminderMap" type="com.example.spring_boot_mode.model.entity.reminder.Reminder">
        <id property="id" column="id"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="targetName" column="target_name"/>
        <result property="userId" column="user_id"/>
        <result property="remindTime" column="remind_time"/>
        <result property="remindMsg" column="remind_msg"/>
        <result property="status" column="status"/>
        <result property="triggerTime" column="trigger_time"/>
        <result property="isOpen" column="is_open"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 插入提醒 -->
    <insert id="insert" parameterType="com.example.spring_boot_mode.model.entity.reminder.Reminder">
        INSERT INTO reminder (
            id, target_type, target_id, target_name, user_id,
            remind_time, remind_msg, status, trigger_time, is_open,
            create_time, update_time
        ) VALUES (
            IFNULL(#{id}, UUID()), #{targetType}, #{targetId}, #{targetName}, #{userId},
            #{remindTime}, #{remindMsg}, #{status}, #{triggerTime}, #{isOpen},
            #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID更新 -->
    <update id="updateById" parameterType="com.example.spring_boot_mode.model.entity.reminder.Reminder">
        UPDATE reminder
        <set>
            <if test="targetType != null">target_type = #{targetType},</if>
            <if test="targetId != null">target_id = #{targetId},</if>
            <if test="targetName != null">target_name = #{targetName},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="remindTime != null">remind_time = #{remindTime},</if>
            <if test="remindMsg != null">remind_msg = #{remindMsg},</if>
            <if test="status != null">status = #{status},</if>
            <if test="triggerTime != null">trigger_time = #{triggerTime},</if>
            <if test="isOpen != null">is_open = #{isOpen},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除 -->
    <delete id="deleteById">
        DELETE FROM reminder WHERE id = #{id}
    </delete>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="ReminderMap">
        SELECT * FROM reminder WHERE id = #{id}
    </select>

    <!-- 分页查询我的提醒列表 -->
    <select id="selectMyList" resultMap="ReminderMap">
        SELECT * FROM reminder
        <where>
            user_id = #{userId}
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="targetType != null and targetType != ''">AND target_type = #{targetType}</if>
            <if test="keyword != null and keyword != ''">
                AND (target_name LIKE CONCAT('%', #{keyword}, '%')
                     OR remind_msg LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY remind_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计我的提醒总数 -->
    <select id="selectMyListTotal" resultType="int">
        SELECT COUNT(*) FROM reminder
        <where>
            user_id = #{userId}
            <if test="status != null and status != ''">AND status = #{status}</if>
            <if test="targetType != null and targetType != ''">AND target_type = #{targetType}</if>
            <if test="keyword != null and keyword != ''">
                AND (target_name LIKE CONCAT('%', #{keyword}, '%')
                     OR remind_msg LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <!-- 查询待触发且已到时间的提醒 -->
    <select id="selectPendingReminders" resultMap="ReminderMap">
        SELECT * FROM reminder
        WHERE status = 'pending'
          AND is_open = 1
          <![CDATA[AND remind_time <= #{beforeTime}]]>
        ORDER BY remind_time ASC
    </select>

    <!-- 根据目标信息查询 -->
    <select id="selectByTarget" resultMap="ReminderMap">
        SELECT * FROM reminder
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          <if test="userId != null and userId != ''">AND user_id = #{userId}</if>
        LIMIT 1
    </select>

    <!-- 根据条件查询单条记录 -->
    <select id="selectOneByCondition" resultMap="ReminderMap">
        SELECT * FROM reminder
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          <if test="userId != null and userId != ''">AND user_id = #{userId}</if>
          <if test="isOpen != null">AND is_open = #{isOpen}</if>
          <if test="status != null and status != ''">AND status = #{status}</if>
        LIMIT 1
    </select>

</mapper>
